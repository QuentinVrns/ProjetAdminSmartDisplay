<Window x:Class="ProjetAdminSmartDisplay.EcranView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ecran" Height="450" Width="800">
    <Grid>
        <!-- Définition des colonnes : une pour les ComboBox, l'autre pour les images -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <!-- Largeur pour les ComboBox -->
            <ColumnDefinition Width="*"/>
            <!-- Largeur flexible pour les images -->
        </Grid.ColumnDefinitions>

        <!-- Partie gauche pour les ComboBox et le texte -->
        <StackPanel Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20">
            <!-- Batiment -->
            <TextBlock Text="Bâtiment ↓" FontSize="16" Margin="0,10,0,5"/>
            <ComboBox x:Name="BatimentComboBox" Width="200" DisplayMemberPath="NomBatiment" 
                      SelectedValuePath="Id" SelectionChanged="OnBatimentSelectionChanged"/>

            <!-- Etage -->
            <TextBlock Text="Etage ↓" FontSize="16" Margin="0,10,0,5"/>
            <ComboBox x:Name="EtageComboBox" Width="200" DisplayMemberPath="NomEtage" 
                      SelectedValuePath="Id" SelectionChanged="OnEtageSelectionChanged"/>

            <!-- Classe -->
            <TextBlock Text="Classe ↓" FontSize="16" Margin="0,10,0,5"/>
            <ComboBox x:Name="ClasseComboBox" Width="200" DisplayMemberPath="NomSalle" 
                      SelectedValuePath="Id" SelectionChanged="OnClasseSelectionChanged"/>

            <!-- Affichage des informations de la classe -->
            <TextBlock x:Name="InfoClasseTextBlock" FontSize="14" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Partie droite pour l'affichage des images -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ImagesControl" HorizontalAlignment="Left" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- WrapPanel pour distribuer les images sur plusieurs lignes -->
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Taille plus grande pour les images -->
                        <Image Source="{Binding}" Width="150" Height="150" Margin="10"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
